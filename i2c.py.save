
t0=time.time()
store_arr=[]
while(count<length):
    bus
t0=time.time()
store_arr=[]
while(count<length):
    bus
t0=time.time()
store_arr=[]
while(count<length):
    bus
import smbus
from time import sleep
bus = smbus.SMBus(1)
addr = 0x08
val = 0x10
while True:
	bus.write_byte(addr,val)
	print(val)
	sleep(1)
t0=time.time()
store_arr=[]
while(count<length):
    bus

